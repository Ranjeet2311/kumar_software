import React from "react";
// import classes from "./DropDown.module.scss";
// import Link from "next/link";

type PlanProps = {
  children: React.ReactNode;
};

export default function DropDown({ children }: PlanProps) {
  return <>{children}</>;
}
